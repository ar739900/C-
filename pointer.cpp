#include <bits/stdc++.h>
using namespace std;

// Node(A) -> Node(B) -> Node(C)
// data + next
class Node
{
    int data;
    Node *next;

public:
    Node(int item)
    {
        this->data = item;
        this->next = NULL;
    }
};

class LinkedList
{
    // 5 -> 10 -> 200 -> 30 -> 40 -> null
    // head             tail
    // size
};

int main()
{
    Node node(10);
    cout << "Done";
}
